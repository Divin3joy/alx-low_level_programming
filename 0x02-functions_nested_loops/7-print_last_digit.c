#include "main.h"
/**
 * print_last_digit - prints the last digit of a value
 * @int: the value to be checked
 * Return: 0
 */

int print_last_digit(int)
{
	int ld = n % 10;

	if (ld < 0)
		ld += -1;
	_putchar(ld + ('0');
	return (0);
}
